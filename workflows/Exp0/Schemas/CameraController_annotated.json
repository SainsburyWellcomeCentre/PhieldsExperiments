{
    "$id": "...h",
    "$schema": "https://json-schema.org/draft-07/schema",
    "description": "Describes the camera controller hardware",
    "title": "CameraControllerSettings",
    "x-sgen-typename": "CameraController.CameraControllerSettings",
    "definitions": {
        "cameraTriggerName": {
            "x-sgen-typename": "CameraController.CameraTriggerName",
            "enum": [
                "Trigger0",
                "Trigger1"
            ],
            "default": "Trigger1",
            "type": "string"
        },
        "cameraTriggerSource": {
            "properties": {
                "triggerName": {
                    "$ref": "#/definitions/cameraTriggerName"
                },
                "triggerFrequency": {
                    "type": "integer",
                    "default": "50"
                }
            },
            "required": [
                "triggerName",
                "triggerFrequency"
            ]
        },
        "cameraControllerName": {
            "enum": [
                "Behavior",
                "CameraControllerGen2",
                "OutputExpander"
            ],
            "default": "CameraControllerGen2",
            "type": "string"
        }
    },
    "properties": {
        "deviceName": {
            "$ref": "#/definitions/cameraControllerName"
        },
        "portName": {
            "type": "string",
            "default": "COM9",
            "description": "The COM port for the camera controller board"
        },
        "triggers": {
            "type": "array",
            "items": {
                "$ref": "#/definitions/cameraTriggerSource"
            }
        }
    },
    "required": [
        "deviceName",
        "portName",
        "triggers"
    ]
}